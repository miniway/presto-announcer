## Python Presto Announcer

    sudo pip install  zope.interface
    sudo pip install twisted
    git clone https://github.com/miniway/twest.git
    cd twest && sudo python setup.py

    twistd announcer -c config.ini
